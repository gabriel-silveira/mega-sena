<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link href="imgs/favicon.ico" rel="shortcut icon" type="image/x-icon" />

  <title>{{ title }}</title>

  <script src="bootstrap/js/bootstrap.min.js"></script>
  <link rel="stylesheet" href="bootstrap/css/bootstrap.min.css">

  <link rel="stylesheet" href="css/geral.css">

  <script src="js/vue.min.js"></script>
  <script src="js/axios.min.js"></script>
  <script src="js/app.js" defer></script>
</head>
<body>
  <div id="app">
    <div class="jumbotron">
      <h1 class="text-primary">{{ title }}</h1>
      <p class="lead">Informe a quantidade de números e apostas nos campos abaixo e clique em gerar número:</p>
      <hr class="my-4">
      <form>
        <div class="row">
          <div class="col-1">
            <div class="form-group mb-0">
              <label for="formGroupExampleInput">Números:</label>
              <input type="text" class="form-control text-center" v-model="qtd_numeros">
            </div>
          </div>
          <div class="col-1">
            <div class="form-group mb-0">
              <label for="formGroupExampleInput">Apostas:</label>
              <input type="text" class="form-control text-center" v-model="qtd_apostas">
            </div>
          </div>
          <div class="col">
            <div class="d-flex align-items-end" style="height:100%">
              <button type="button" class="btn btn-success mr-4" @click="sortear_numeros()">Gerar números!</button>
              <button type="button" class="btn btn-outline-secondary" @click="limpar()">Limpar</button>
            </div>            
          </div>
        </div>
      </form>
    </div>
    <div v-if="numeros_sorteados.length > 0" class="card ml-4 mr-4 mb-5">
      <h5 class="card-header text-center">Sorteados</h5>
      <div class="card-body">
        <p class="card-text">
          <div v-for="numeros in numeros_sorteados" class="apostas text-center">
            <span v-for="n in numeros">[[ n ]]</span>
          </div>
        </p>
        <p class="card-text text-center pt-0 mt-0"><small class="text-muted">Sorteado em 24 de maio de 2019 às 13:30h</small></p>
      </div>
    </div>
    <footer class="bg-primary">Desenvolvido por <a href="http://www.gabrielsilveira.com.br" target="_blank">Gabriel Silveira</a></footer>
  </div>
</body>
</html>